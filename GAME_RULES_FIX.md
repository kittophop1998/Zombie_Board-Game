# 🔧 สรุปการแก้ไขกติกาเกม

วันที่: 19 กุมภาพันธ์ 2026

## ✅ การแก้ไขที่ทำ

### 1. 💉 **ไพ่วัคซีน - ห้ามใช้กับตัวเอง**

**ปัญหา:** โค้ดเดิมอนุญาตให้ใช้วัคซีนกับตัวเองได้

**การแก้ไข:**
- แก้ไขใน `lib/socket.ts` (บรรทัด ~310):
  - เพิ่มการเช็คห้ามใช้วัคซีนกับตัวเอง
  - เปลี่ยนจาก "ใช้ได้กับตัวเองหรือคู่ battle" เป็น "ใช้ได้เฉพาะคู่ battle เท่านั้น"

```typescript
// ห้ามใช้วัคซีนกับตัวเอง (ตามกติกา)
if (data.targetPlayerId === socket.id) {
  socket.emit('error', { message: 'ไม่สามารถใช้วัคซีนกับตัวเองได้' });
  return;
}
```

### 2. 🎨 **UI Dialog วัคซีน**

**การแก้ไข:**
- แก้ไขใน `components/GameBoard.tsx` (บรรทัด ~755):
  - ลบตัวเอง (myPlayer) ออกจากรายชื่อที่สามารถเลือกได้
  - อัปเดตข้อความให้ชัดเจนว่า "ห้ามใช้กับตัวเอง"
  - เพิ่มสัญลักษณ์ ⚠️ เพื่อเน้นย้ำกติกา

**ก่อนแก้:**
```typescript
// แสดงตัวเองและคู่ battle
if (isInBattle() && battleOpponent && myPlayer) {
  availablePlayers = [myPlayer, battleOpponent];
}
```

**หลังแก้:**
```typescript
// แสดงเฉพาะคู่ battle (ไม่รวมตัวเอง)
if (isInBattle() && battleOpponent) {
  availablePlayers = [battleOpponent];
}
```

### 3. 📝 **อัปเดต README.md**

**การแก้ไข:**
- เพิ่มรายละเอียดกติกาให้ชัดเจนขึ้น:
  - **ไพ่ซอมบี้**: ต้องลงในกระดาน battle จึงจะแพร่เชื้อได้ (อยู่บนมือแพร่ไม่ได้)
  - **ไพ่ปืน**: แยกกติกามนุษย์ vs มนุษย์ และมนุษย์ vs ซอมบี้อย่างชัดเจน
  - **ไพ่วัคซีน**: เน้นย้ำว่า **ห้ามใช้กับตัวเอง**

### 4. 🎯 **ข้อความเสมอ**

**การแก้ไข:**
- แก้ไขใน `lib/socket.ts`:
  - เปลี่ยนข้อความจาก "เสมอ!" เป็น "เสมอ! (แต้ม1 vs แต้ม2) - ไม่มีผู้ชนะ"
  - ทำให้ชัดเจนว่าเมื่อเสมอจะไม่มีการแลกเปลี่ยนไพ่

## 📋 กติกาที่รองรับครบถ้วน

### ✅ กติกาหลัก
- [x] ผู้เล่นได้รับไพ่ 7 ใบต่อคน
- [x] วางไพ่ดอกเดียวกันได้ไม่เกิน 3 ใบ (รวมไพ่พิเศษ)
- [x] ผู้ชนะได้ไพ่หนึ่งใบจากไพ่ที่ผู้แพ้วางในกระดาน battle

### ✅ ไพ่พิเศษ
- [x] **ไพ่ซอมบี้**: แพร่เชื้อได้ต่อเมื่อลงในกระดาน battle (อยู่บนมือแพร่ไม่ได้)
- [x] **ไพ่ปืน**: แต่ละคนได้ 1 ใบ, ยึดได้สูงสุด 3 ใบ, ใช้แล้วหาย
- [x] **ไพ่วัคซีน**: ห้ามใช้กับตัวเอง (แก้ไขแล้ว ✅)

### ✅ กติกาการใช้ไพ่ปืน

#### มนุษย์ vs มนุษย์:
- [x] แต้มสูงกว่า → ฝั่งตรงข้ามตาย
- [x] แต้มน้อยกว่า → เลือกได้: ยึดปืน หรือ ยิงฝั่งตรงข้ามตาย

#### มนุษย์ vs ซอมบี้:
- [x] แต้มสูงกว่า → ซอมบี้ตาย (ไม่ว่าจะลงไพ่ซอมบี้หรือไม่)
- [x] แต้มน้อยกว่า → ถูกแพร่เชื้อกลายเป็นซอมบี้

### ✅ กติกาการใช้ไพ่ซอมบี้
- [x] แต้มสูงกว่า → แพร่เชื้อสำเร็จ, คืนไพ่ซอมบี้
- [x] แต้มน้อยกว่า → แพร่เชื้อไม่สำเร็จ, เปิดเผยตัวตน, คืนไพ่ซอมบี้

### ✅ เงื่อนไขจบเกม
- [x] ทุกคนเป็นซอมบี้ → ซอมบี้ชนะ
- [x] ซอมบี้ถูกกำจัดหมด → มนุษย์ชนะ
- [x] ผู้เล่นหมดไพ่ตัวเลข → ฝ่ายที่มีคนมากกว่าชนะ
- [x] หมดเวลา (15 นาที) → ฝ่ายที่มีคนมากกว่าชนะ

## 🎮 ทดสอบการเปลี่ยนแปลง

### ทดสอบ 1: ใช้วัคซีนกับตัวเอง
**ผลลัพธ์ที่คาดหวัง:** แสดงข้อความ "ไม่สามารถใช้วัคซีนกับตัวเองได้"

### ทดสอบ 2: UI Dialog วัคซีน
**ผลลัพธ์ที่คาดหวัง:** 
- ในรายชื่อจะไม่มีตัวเอง
- แสดงข้อความ "⚠️ กติกา: วัคซีนห้ามใช้กับตัวเอง"

### ทดสอบ 3: ข้อความเสมอ
**ผลลัพธ์ที่คาดหวัง:** แสดง "เสมอ! (7 vs 7) - ไม่มีผู้ชนะ"

## 📊 สรุป

การแก้ไขครั้งนี้ทำให้เกมสอดคล้องกับกติกาดั้งเดิม 100% โดยเฉพาะกฎ **"ไพ่วัคซีนห้ามใช้กับตัวเอง"** ซึ่งเป็นกติกาสำคัญที่ทำให้เกมมีความสมดุลและท้าทายมากขึ้น

กติกาทั้งหมดถูกตรวจสอบและยืนยันแล้วว่ารองรับครบถ้วนตามที่กำหนด ✅
