# การอัพเดทระบบเกมซอมบี้ - กติกาฉบับสมบูรณ์

## 📋 สรุปกติกาทั้งหมด

### 1. การวางไพ่ ✅
- **วางได้ไม่เกิน 4 ใบต่อ battle** (รวมไพ่พิเศษ: ซอมบี้, ปืน)
- **ต้องวางดอกเดียวกัน** (เฉพาะไพ่ตัวเลข)
- ไพ่พิเศษไม่นับดอก สามารถวางร่วมกับไพ่ตัวเลขได้

### 2. กติกาการใช้ไพ่ปืน 🔫

#### 2.1 มนุษย์ใช้ปืน vs มนุษย์
- **แต้มสูงกว่า** → ฝั่งตรงข้ามตาย (ELIMINATED)
- **แต้มน้อยกว่า** → ฝั่งตรงข้ามเลือกได้:
  - ยึดปืนมาใช้เอง (เก็บได้สูงสุด 3 ใบ)
  - หรือ ยิงฝั่งตรงข้ามกลับ (ทำให้ตาย)

#### 2.2 มนุษย์ใช้ปืน vs ซอมบี้
- **แต้มสูงกว่า** → ซอมบี้ตาย (ไม่ว่าจะลงไพ่ซอมบี้หรือไม่)
- **แต้มน้อยกว่า** → ถูกแพร่เชื้อกลายเป็นซอมบี้

### 3. กติกาการใช้ไพ่ซอมบี้ 🧟

#### 3.1 ซอมบี้ใช้ไพ่ซอมบี้ vs มนุษย์
- **แต้มสูงกว่า** → แพร่เชื้อสำเร็จ + **คืนไพ่ซอมบี้กลับ**
- **แต้มน้อยกว่า** → แพร่เชื้อไม่สำเร็จ + **เปิดเผยตัวตนว่าเป็นซอมบี้** + **คืนไพ่ซอมบี้กลับ**
- **เสมอ** → **คืนไพ่ซอมบี้กลับ**

#### 3.2 ซอมบี้ vs ซอมบี้
- นับคะแนนตามปกติ (ไม่มีการแพร่เชื้อ)

### 4. กฎไพ่ปืน 🎯
- มนุษย์ทุกคนเริ่มต้นมีไพ่ปืน **1 ใบ**
- ใช้ได้ **ครั้งเดียว** เท่านั้น (ยกเว้นยึดได้จากคนอื่น)
- **เก็บได้สูงสุด 3 ใบ**

---

## 🔧 การเปลี่ยนแปลงในโค้ด

### 1. `lib/gameLogic.ts`

#### เพิ่มฟังก์ชันใหม่:
```typescript
// ตรวจสอบว่าไพ่เป็นดอกเดียวกันหรือไม่
export function checkSameSuit(cards: Card[]): boolean
```

#### อัพเดทฟังก์ชัน `determineBattleWinner`:
```typescript
export function determineBattleWinner(
  cards1: Card[],
  cards2: Card[],
  player1: Player,
  player2: Player
): {
  winnerId: string | null;
  isInfection: boolean;
  infectedPlayerId?: string;
  zombiePlayerId?: string;
  zombieCardReturned?: boolean; // ใหม่: คืนไพ่ซอมบี้
  eliminatedPlayerId?: string; // ใหม่: ผู้เล่นที่ตาย
  shotgunAction?: 'stolen' | 'kill_opponent' | 'kill_self'; // ใหม่: การกระทำของปืน
  shotgunOwnerId?: string; // ใหม่: เจ้าของปืนใหม่
  zombieRevealed?: boolean; // ใหม่: ซอมบี้ถูกเปิดเผย
}
```

**Logic ใหม่:**
- ✅ จัดการกติกาไพ่ปืน (มนุษย์ vs มนุษย์, มนุษย์ vs ซอมบี้)
- ✅ จัดการการแพร่เชื้อของซอมบี้ (สำเร็จ/ไม่สำเร็จ)
- ✅ คืนไพ่ซอมบี้กลับเมื่อใช้แล้ว
- ✅ เปิดเผยตัวตนซอมบี้เมื่อแพร่เชื้อไม่สำเร็จ

### 2. `lib/socket.ts`

#### อัพเดท `play-card`:
```typescript
// เช็ควางไพ่ไม่เกิน 4 ใบ
if (myCards.length >= 4) {
  socket.emit('error', { message: 'วางไพ่ได้สูงสุด 4 ใบ (รวมไพ่พิเศษ)' });
  return;
}

// เช็คดอกไพ่เดียวกัน
const numberCards = tempCards.filter(c => c.type === 'NUMBER' && c.suit);
if (numberCards.length > 1) {
  const firstSuit = numberCards[0].suit;
  const sameSuit = numberCards.every(c => c.suit === firstSuit);
  if (!sameSuit) {
    socket.emit('error', { message: 'ต้องวางไพ่ดอกเดียวกัน!' });
    return;
  }
}

// เช็คไพ่ปืนไม่เกิน 3 ใบ
if (card.type === 'SHOTGUN') {
  const shotgunCount = player.cards.filter(c => c.type === 'SHOTGUN').length;
  if (shotgunCount > 3) {
    socket.emit('error', { message: 'เก็บไพ่ปืนได้สูงสุด 3 ใบ' });
    return;
  }
}
```

#### อัพเดท `resolveBattle`:
```typescript
function resolveBattle(room: GameRoom, battle: Battle, io: SocketServer) {
  // คำนวณผล
  const result = determineBattleWinner(...);
  
  // จัดการกรณีพิเศษ:
  
  // 1. กำจัด (ยิงตาย)
  if (result.eliminatedPlayerId) { ... }
  
  // 2. ยึดปืน
  if (result.shotgunAction === 'stolen' && result.shotgunOwnerId) {
    // ตรวจสอบไม่เกิน 3 ใบ
    if (currentShotguns < 3) { ... }
  }
  
  // 3. แพร่เชื้อ
  if (result.isInfection && result.infectedPlayerId) { ... }
  
  // 4. เปิดเผยซอมบี้
  if (result.zombieRevealed && result.zombiePlayerId) { ... }
  
  // 5. คืนไพ่ซอมบี้
  if (result.zombieCardReturned && result.zombiePlayerId) {
    zombiePlayer.cards.push(zombieCard);
  }
  
  // 6. ผู้ชนะได้ไพ่จากผู้แพ้
  if (winnerId) { ... }
}
```

### 3. `types/game.ts`
- ไม่มีการเปลี่ยนแปลง (ใช้โครงสร้างเดิม)

---

## ✅ ผลลัพธ์

### สิ่งที่ทำได้:
1. ✅ วางไพ่ได้ไม่เกิน 4 ใบ (รวมไพ่พิเศษ)
2. ✅ ต้องวางไพ่ดอกเดียวกัน
3. ✅ กติกาไพ่ปืนครบถ้วน (มนุษย์ vs มนุษย์, มนุษย์ vs ซอมบี้)
4. ✅ การยึดปืน (เก็บได้สูงสุด 3 ใบ)
5. ✅ การแพร่เชื้อซอมบี้ (สำเร็จ/ไม่สำเร็จ)
6. ✅ **คืนไพ่ซอมบี้กลับเมื่อใช้แล้ว**
7. ✅ เปิดเผยตัวตนซอมบี้เมื่อแพร่เชื้อไม่สำเร็จ
8. ✅ หลาย battle พร้อมกันได้

### ⚠️ สิ่งที่ต้องทำต่อ (Client Side):
- **UI สำหรับเลือก**: เมื่อมนุษย์ใช้ปืนแพ้ ฝั่งตรงข้ามต้องเลือกว่าจะ "ยึดปืน" หรือ "ยิงกลับ"
  - ต้องสร้าง dialog/modal ให้ผู้เล่นเลือก
  - ส่ง action กลับมา server เพื่อจัดการต่อ

---

## � ตัวอย่างการใช้งาน

### กรณีที่ 1: ซอมบี้แพร่เชื้อสำเร็จ
```
ซอมบี้ A: ไพ่ซอมบี้ + ไพ่ 7 = 7 แต้ม
มนุษย์ B: ไพ่ 5 = 5 แต้ม
→ ซอมบี้ A ชนะ
→ มนุษย์ B ติดเชื้อ (ได้ไพ่ซอมบี้ใหม่)
→ ไพ่ซอมบี้ของ A กลับคืนมา ✅
```

### กรณีที่ 2: ซอมบี้แพร่เชื้อไม่สำเร็จ
```
ซอมบี้ A: ไพ่ซอมบี้ + ไพ่ 5 = 5 แต้ม
มนุษย์ B: ไพ่ 8 = 8 แต้ม
→ มนุษย์ B ชนะ
→ ซอมบี้ A ถูกเปิดเผยตัวตน
→ ไพ่ซอมบี้ของ A กลับคืนมา ✅
```

### กรณีที่ 3: มนุษย์ใช้ปืนแพ้
```
มนุษย์ A: ไพ่ปืน + ไพ่ 3 = 3 แต้ม
มนุษย์ B: ไพ่ 7 = 7 แต้ม
→ มนุษย์ B ชนะ
→ มนุษย์ B เลือกได้:
  1. ยึดปืน (ได้ไพ่ปืน 1 ใบ)
  2. ยิงกลับ (มนุษย์ A ตาย)
```

### กรณีที่ 4: มนุษย์ใช้ปืนกับซอมบี้แพ้
```
มนุษย์ A: ไพ่ปืน + ไพ่ 4 = 4 แต้ม
ซอมบี้ B: ไพ่ 9 = 9 แต้ม
→ ซอมบี้ B ชนะ
→ มนุษย์ A ติดเชื้อ ✅
```

---

## 🎮 สรุป
กติกาครบถ้วนและพร้อมใช้งาน! ระบบจัดการการแพร่เชื้อ, การคืนไพ่ซอมบี้, และกติกาไพ่ปืนครบถ้วนแล้ว 🎉

